# Hardware-Optimized Environment Configuration
# AMD Ryzen 9 3900X + RTX 3080 + 32GB RAM

# =============================================================================
# HARDWARE OPTIMIZATION SETTINGS
# =============================================================================

# Ollama Configuration (Desktop-Optimized)
OLLAMA_NUM_PARALLEL=3              # Multiple models simultaneously
OLLAMA_TIMEOUT=60                   # Faster timeouts for better hardware
OLLAMA_GPU_LAYERS=35               # Use GPU for most layers
OLLAMA_CPU_LAYERS=5                # Keep some layers on CPU for stability

# GPU Acceleration
CUDA_VISIBLE_DEVICES=0             # Use RTX 3080
TORCH_CUDA_ARCH_LIST=8.6           # RTX 3080 architecture
CUDA_LAUNCH_BLOCKING=0             # Non-blocking CUDA operations

# Memory Management
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:512  # Optimize GPU memory allocation
OMP_NUM_THREADS=12                 # Use all CPU cores
MKL_NUM_THREADS=12                 # Optimize math operations

# =============================================================================
# SERVICE CONFIGURATION (OPTIMIZED FOR DESKTOP)
# =============================================================================

# FastAPI Configuration
PORT=8008
HOST=127.0.0.1
WORKERS=4                          # Multiple workers for 12-core CPU
LOG_LEVEL=info
MAX_REQUESTS=1000                  # Higher request limits
TIMEOUT=30                         # Faster timeouts

# Gradio UI Configuration
UI_PORT=7860
UI_SHARE=false
UI_DEBUG=false                     # Disable debug for performance
UI_MAX_THREADS=8                   # Multiple UI threads
UI_QUEUE_SIZE=20                   # Larger request queue

# =============================================================================
# PIPELINE OPTIMIZATION
# =============================================================================

# Parallel Processing
MAX_WORKERS=4                      # Multiple pipeline workers
BATCH_SIZE=8                       # Larger batch processing
CONCURRENT_DOWNLOADS=6             # Multiple concurrent downloads

# Video Processing
VIDEO_QUALITY=high                 # Use RTX 3080 for encoding
GPU_ACCELERATION=true              # Enable GPU acceleration
FFMPEG_THREADS=12                  # Use all CPU cores for video

# Asset Processing
CACHE_SIZE_GB=4                    # Larger asset cache
ASSET_CONCURRENCY=6                # Multiple asset operations

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================
ADMIN_TOKEN=default-admin-token-change-me

# =============================================================================
# DATABASE & STORAGE
# =============================================================================
DATABASE_URL=sqlite:///./data/dev.db
STORAGE_PATH=./data
CACHE_PATH=./data/cache

# =============================================================================
# EXTERNAL APIs (OPTIONAL)
# =============================================================================
OPENAI_API_KEY=
GOOGLE_API_KEY=

# =============================================================================
# PERFORMANCE MONITORING
# =============================================================================
ENABLE_METRICS=true
PERFORMANCE_TRACKING=true
PROFILING=true
