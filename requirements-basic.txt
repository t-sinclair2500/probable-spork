# Basic requirements for the probable-spork pipeline
# Core dependencies that should work on most systems

# Image processing (for thumbnail generation fallback)
Pillow>=9.0.0
cairosvg>=2.7.0

# Core dependencies
requests>=2.28.0
pyyaml>=6.0
jinja2>=3.1.0
python-dotenv>=0.19.0

# Optional: For better SVG rendering (if available)
# rsvg-convert (system package)
# ImageMagick (system package)
